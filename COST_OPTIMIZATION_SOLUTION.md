# RAG系统低成本预筛选模型解决方案

## 问题描述

原有的RAG系统在召回阶段返回过多切片，导致tokens浪费，特别是在重排序（rerank）阶段消耗大量昂贵的API资源。

## 解决方案概述

我们实现了一个低成本预筛选模型，该模型在将切片发送给昂贵的重排序模型之前，使用轻量级算法评估切片与查询的相关性置信度，从而减少不必要的API调用。

## 技术实现

### 1. 低成本预筛选模型 (`low_cost_filter.py`)

- 使用TF-IDF和关键词重叠相结合的方法评估相关性
- 实现了置信度评分机制
- 提供了动态阈值过滤功能

### 2. 算法原理

- **关键词重叠度**：使用Jaccard相似度计算查询和切片的词汇重叠
- **语义相似度**：使用TF-IDF向量计算余弦相似度
- **组合评分**：关键词重叠占40%，语义相似度占60%

### 3. 集成到现有RAG系统

- 修改了`RAG_Backend.py`中的`RecallWorker`类
- 添加了低成本预筛选的开关和参数配置
- 在重排序前应用预筛选

## 核心优势

1. **成本节约**：大幅减少发送给重排序模型的切片数量
2. **性能提升**：减少API调用，加快整体响应时间
3. **灵活性**：可调节的置信度阈值和最大切片数
4. **兼容性**：与现有RAG系统无缝集成

## 配置参数

- `enable_low_cost_filter`：启用/禁用预筛选功能
- `confidence_threshold`：置信度阈值（0-1之间）
- `max_chunks_after_filter`：预筛选后最大切片数量

## 测试结果

使用测试数据验证了模型的有效性：
- 原始8个切片，通过置信度阈值0.05筛选后保留3个
- 处理时间约1.5秒，效率较高
- 不同阈值测试显示了良好的可控性

## 行业案例参考

1. **Meta REF-RAG**：通过强化学习策略评估片段相关性，只发送最相关的片段给LLM
2. **NVIDIA重排序微服务**：通过减少输入tokens数量来降低RAG成本
3. **CRAG（Corrective RAG）**：使用轻量级检索评估器为文档分配置信度评分

## 部署说明

1. 安装依赖：`pip install scikit-learn`
2. 确保系统中有jieba分词库
3. 在RAG系统中启用预筛选功能
4. 根据实际需求调整置信度阈值

## 效果预期

通过部署此低成本预筛选模型，预计可以：
- 减少60-80%的重排序API调用
- 降低相应的tokens消耗成本
- 提高RAG系统的整体响应速度
- 保持检索质量不受显著影响