# RAG Code Buddy 用户手册

## 快速入门

### 系统要求
- Python 3.9 或更高版本
- 至少4GB内存
- 稳定的互联网连接（用于API调用）

### 安装步骤

1. **安装Python依赖**
```bash
pip install -r requirements.txt
```

2. **配置API密钥**
创建 `.env` 文件并添加：
```
QWEN_API_KEY=your_qwen_api_key
BGE_API_KEY=your_bge_api_key
BGE_RERANK_API_KEY=your_bge_rerank_api_key
```

3. **启动系统**
```bash
python front_pgui.py
```

## 界面介绍

### 主界面
系统包含三个主要标签页：
- **TAB A**: PDF解析与PageIndex构建
- **TAB B**: RAG向量化处理（LLM驱动）
- **TAB C**: 规则基础转换（无LLM，高速）

## 详细操作指南

### TAB A: PDF解析

#### 功能说明
将PDF文档转换为结构化的PageIndex JSON文件，为后续处理做准备。

#### 操作步骤
1. 点击 **"📂 LOAD PDF"** 按钮，选择要处理的PDF文件
2. 从下拉菜单中选择合适的AI模型（推荐：qwen-plus）
3. 选择处理策略：
   - **Schema A**: 完整智能处理
   - **Schema B**: 数据仓库模式
   - **Schema C**: 仅结构提取
   - **IATA Hybrid**: 高速混合模式
   - **IATA Keywords**: 关键词优化模式
   - **IATA Batch**: 批处理模式
4. 设置并发线程数（1-10）
5. 点击 **"🚀 START INDEXING (OPTIMIZED)"** 开始处理

#### 输出结果
- 生成 `*.json` 文件（PageIndex格式）
- 包含文档结构、章节、内容等信息

### TAB B: RAG向量化 (LLM驱动)

#### 功能说明
使用大语言模型将PageIndex JSON转换为适合RAG系统的向量化格式。

#### 操作步骤
1. 点击 **"📂 SELECT JSON"** 选择TabIndex生成的JSON文件
2. 系统会自动生成输出路径（格式：`RAGjson_*.json`）
3. 选择处理模型（推荐：qwen-plus）
4. 选择处理策略：
   - **0: 数据无损模式**: 保留完整数据，适合表格/时间表
   - **1: 公文语义总结**: 生成语义摘要，适合政策/文档
   - **2: 标准混合模式**: 平衡语义和数据保留
5. 点击 **"⚡ GENERATE VECTOR JSON"** 开始处理

#### 输出结果
- 生成 `RAGjson_*.json` 文件
- 包含语义导语、元数据、原始内容等
- 准备好用于向量化和检索

### TAB C: 规则基础转换 (高速)

#### 功能说明
使用规则基础的方法快速将PageIndex JSON转换为RAG格式，无需调用LLM，处理速度更快。

#### 操作步骤
1. 点击 **"📂 SOURCE"** 选择PageIndex JSON文件
2. 系统自动设置输出路径（格式：`vector_RAG_*.json`）
3. 点击 **"⚡ RUN CONVERTER (RULE BASED)"** 开始转换

#### 输出结果
- 生成 `vector_RAG_*.json` 文件
- 格式与TAB B输出兼容
- 适合快速处理大批量文档

## RAG查询系统

### 使用流程
1. 完成TAB A → TAB B/C的文档处理流程
2. 启动RAG前端（如RAG_Frontend.py）
3. 输入查询问题
4. 系统将返回相关答案和来源信息

### 查询示例
- "飞机发动机维护步骤是什么？"
- "燃油系统故障排查方法？"
- "紧急情况处理程序"

## BGE向量化客户端 (bge_gui.py)

### 功能说明
专门的向量化和重排序工具，可以直接处理JSON文件并生成向量数据库。

### 操作步骤
1. 运行 `python bge_gui.py`
2. 选择JSON文件
3. 选择操作类型：
   - **向量化**: 生成向量数据库
   - **重排序**: 对检索结果进行重排序
4. 点击 **"🚀 开始发送 BGE 向量化 & 重排序"** 

## 常见问题解答

### Q: API密钥在哪里获取？
A: 
- Qwen API: 阿里云DashScope平台
- BGE API: SiliconFlow平台
- 需要注册账户并获取相应的API密钥

### Q: 处理大型PDF文档很慢怎么办？
A: 
- 使用TAB C的规则基础转换（速度最快）
- 选择更高的并发线程数
- 考虑分批处理大文档

### Q: 如何提高检索准确度？
A: 
- 优化查询问题的表达
- 使用TAB B的语义总结模式处理文档
- 调整重排序参数

### Q: 出现API错误怎么办？
A: 
- 检查API密钥是否正确
- 确认API服务是否正常
- 检查网络连接

## 性能优化建议

### 处理大型文档
- 分割大文档为较小部分
- 使用高速模式（TAB C）
- 增加并发处理

### API使用优化
- 合理规划API调用频率
- 使用缓存减少重复调用
- 监控API使用量

### 系统资源优化
- 确保足够的内存空间
- 使用SSD存储提升I/O性能
- 关闭不必要的后台程序

## 故障排除

### 常见错误及解决方案

1. **API密钥错误**
   - 检查 `.env` 文件配置
   - 确认API密钥有效性
   - 验证API服务状态

2. **内存不足**
   - 减少并发线程数
   - 分批处理大文档
   - 增加虚拟内存

3. **处理失败**
   - 检查文件格式兼容性
   - 查看详细错误日志
   - 尝试不同的处理策略

## 高级功能

### 批量处理
- 使用脚本自动化处理流程
- 创建处理队列
- 监控批量任务状态

### 自定义配置
- 修改处理参数
- 调整模型设置
- 优化处理策略

## 技术支持

### 获取帮助
- 查看日志输出
- 检查系统状态
- 联系技术支持

### 反馈建议
- 报告bug或问题
- 提出功能改进建议
- 分享使用经验

---

**手册版本**: 1.0  
**更新日期**: 2026-01-18  
**适用系统**: RAG Code Buddy DEV0